# Tor Huus' dotfiles

Install script and instructions will follow.

### Required/should be installed
* fzf
* ripgrep
* build-essentials (not needed, but installs a few good deps like 'make' c compiler)
* nvm for node version management
* general apt update/uprade
* prettierd executable is required for null-ls npm install -g @fsouza/prettierd
